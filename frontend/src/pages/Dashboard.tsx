import React from 'react';
import { Page } from "../components";

export const Dashboard = () => {
    return (
        <Page title={ 'Dashboard' }>
            Hello Dashboard
        </Page>
    );
};
